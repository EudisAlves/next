import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    BoxTitle: {
      height: 90,
      backgroundColor: '#444',    
    },
    text: {
      color: '#fff',
      fontSize: 25,
      marginTop: 50,
      marginLeft: 25,
    }
  });

export default styles;