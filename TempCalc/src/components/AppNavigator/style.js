import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    box: {
        flex:1,
        padding: 10,
        backgroundColor: '#444'
    },
    iconWrapper: {
        margin: '10',
    }
  });

export default styles;