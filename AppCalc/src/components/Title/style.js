import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    boxTitle: {
        margin:20,
        marginTop: 45,
        padding: 5,
        borderColor: '#fff',
        borderWidth: 0.5,
        flexDirection:'row'
    },
    text: {
        color: '#fff',
        marginTop: 30,
        fontSize: 25,
        fontWeight: 500,
    },
    img: {
        width: 100, 
        height: 100,
        marginLeft: 125,
    }
  })

export default styles;